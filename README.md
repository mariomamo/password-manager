# password-manager
Install neutralino js

```bash
npm install -g @neutralinojs/neu
```

## Install dependencies
```bash
cd app && npm install
```

## Build application
```bash
cd app && npm run build
```
